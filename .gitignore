# secrets
.env
*.pem
razorpay_test_api_keys_*.csv

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# node
node_modules/
dist/
.vite/

# data / logs
data/*.db
data/*.sqlite3
logs/
*.log

# Day 4 model-provider disk cache -- real, gitignored responses, not committed data.
# NOT a wildcard on data/*.json: backend/data/harvested_corpus.json and the committed
# playbook_*.json files are real, tracked data and must stay tracked.
backend/data/model_cache/

# playwright MCP screenshots/snapshots -- session-local verification artifacts, not deliverables
.playwright-mcp/

# os / editor
.DS_Store
.vscode/*
!.vscode/extensions.json

# local Claude Code settings/tooling -- development-environment configuration, kept
# locally, not submission content. A committed copy reads as carelessness, not intent.
.claude/settings.local.json
.claude/commands/

# Prompt transcripts and interview-prep material -- real working documents, kept
# locally, but a transcript of instructing an AI ("paste everything below the line")
# and first-person interview rehearsal notes are not submission content. Deleted from
# tracking, not deleted from disk -- see docs/README.md for where their real content
# now lives publicly (architecture.md, docs/results.md, docs/ENGINEERING-DOCTRINE.md).
docs/day4prompt.md
docs/day5surfaceplan.md
interviewfolderspec.md
interview/
