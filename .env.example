# Copy this file to .env and fill in real values. Never commit .env itself.
# Then load it into your shell before opening VS Code — see SETUP.md step 3.

# Razorpay TEST MODE keys — dashboard.razorpay.com -> Settings -> API Keys -> Test Mode
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Derived, not typed by hand: base64("$RAZORPAY_KEY_ID:$RAZORPAY_KEY_SECRET")
# Generate with:  echo -n "$RAZORPAY_KEY_ID:$RAZORPAY_KEY_SECRET" | base64
# (PowerShell:  [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("$env:RAZORPAY_KEY_ID`:$env:RAZORPAY_KEY_SECRET")) )
RAZORPAY_MCP_TOKEN=

# console.anthropic.com -> API keys. Optional -- not currently wired to any code
# path (classification is deterministic, per CLAUDE.md; grep confirms no backend
# code calls Anthropic today). Kept for a future surface, not a stale claim.
ANTHROPIC_API_KEY=

# context7.com dashboard -> API key. Optional — raises the free rate limit. Leave blank to try without one first.
CONTEXT7_API_KEY=

# GitHub Personal Access Token, repo scope — only needed if you wire up the github MCP server
GITHUB_PAT=

# Day 4 model layer (app/model/) -- playbook synthesis + provider bake-off.
# Both free, no card required.
# aistudio.google.com/apikey -- Gemini free tier (trains on submitted content by
# default; fine here since synthesis input is aggregate statistics from our own run,
# never real customer data -- see docs/results.md's Day 4 section).
GEMINI_API_KEY=
# console.groq.com/keys -- Groq free tier (30 req/min on openai/gpt-oss-120b,
# confirmed via console.groq.com/docs/rate-limits; agreement excludes training on
# submitted content by default, no free/paid split).
GROQ_API_KEY=
# gemini | groq -- selects the default provider in app.model.provider.get_provider()
# when no explicit name is passed. Leave blank to require an explicit choice.
MODEL_PROVIDER=

# Local dev database. Left blank on purpose: a relative sqlite:///./data/recoup.db
# resolves against whatever directory the process was launched from — repo root vs
# backend/ (CLAUDE.md's own documented `cd backend && uvicorn ...`) land in two
# different files. backend/app/db.py's default already computes an absolute path to
# the repo-root data/recoup.db regardless of cwd; only override this if you deliberately
# want a different location.
DATABASE_URL=
